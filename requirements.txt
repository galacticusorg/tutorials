# Python packages used in this tutorial.
#
# You will install these yourself during the tutorial (that is part of the
# exercise!), but this file records exactly what is needed and pins major
# versions for reproducibility. To install everything at once instead:
#
#     pip install -r requirements.txt
#
# See tutorial/01-setup-and-run.md for the step-by-step version.

# The Galacticus version is pinned so that future releases cannot break the
# tutorial: the launcher version selects the matching release (v0.9.12) of the
# executable and datasets.
galacticus==0.9.12           # installs & runs the Galacticus model (downloads a prebuilt binary on first run)
dendros[pandas,tabulate,plot]>=0.5,<1.0   # reads & analyses Galacticus HDF5 output
plotly>=5.0                 # interactive 3D visualisation of the subhalo distribution
nbformat>=4.2               # lets Plotly render figures inside the notebook
jupyter>=1.0                # notebook interface (already present in the Codespace image)
